1. git add --all : to add all the previously done changes into the local cloned workspace.
2. git commit -m "commit message" : to commit a change and give it a message along with the message given.
3. git push : to push all the changes for a pull request.
check