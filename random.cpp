#include<bits/stdc++.h>

using namespace std;

int findMinWindow(string s, int k) {
    int n = s.size();
    int i = 0, j = 0;
    int ans = INT_MAX;
    set<int> st;
    int cnt = 0;
    
   
    return ans;
}